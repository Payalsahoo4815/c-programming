#include<stdio.h>
int main()
{
    float p ,q , sum ;
    printf("Enter the first number");
    scanf("%f",&p);
    printf("Enter the second number ");
    scanf("%f",&q);
    sum = p+q;
    printf("sum of two number is %f",sum);

    return 0;
}